{
  "name": "@mycelium/relayer",
  "version": "0.1.0",
  "description": "Gasless GToken purchase relayer — designed to evolve into a micro-bundler",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "test:watch": "vitest",
    "type-check": "tsc --noEmit"
  },
  "dependencies": {
    "viem": "^2.21.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241205.0",
    "typescript": "^5.7.3",
    "vitest": "^2.1.9",
    "wrangler": "^3.103.2"
  }
}

Research highlight 1

Research highlight 2

# Example Section

Section text. See Subsection [1.1](#subsec1).

## Example Subsection

Subsection text.

### Mathematics

This is an example for the symbol $`\alpha`$ tagged as inline mathematics.

``` math
f(x) = (x+a)(x+b)
```

``` math
f(x) = (x+a)(x+b)
```

``` math
\begin{aligned}
 f(x) &= (x+a)(x+b) \\
      &= x^2 + (a+b)x + ab
\end{aligned}
```

``` math
\begin{aligned}
 f(x) &=& (x+a)(x+b) \nonumber\\ %% If equation numbering is not needed for a row use \nonumber.
      &=& x^2 + (a+b)x + ab
\end{aligned}
```

``` math
\begin{aligned}
 f(x) &= (x+a)(x+b) \\
      &= x^2 + (a+b)x + ab
\end{aligned}
```

``` math
\begin{aligned}
 f(x)&=& (x+a)(x+b) \\
     &=& x^2 + (a+b)x + ab
\end{aligned}
```

|     |     |     |
|:----|:---:|----:|
| 1   |  2  |   3 |
| 4   |  5  |   6 |
| 7   |  8  |   9 |

Table Caption

![](example-image-a)

Figure Caption

# Example Appendix Section

Appendix text.

Example citation, See .

00

Leslie Lamport, *LaTeX: a document preparation system*, Addison Wesley, Massachusetts, 2nd edition, 1994.
